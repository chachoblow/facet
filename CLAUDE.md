# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Status

Monorepo scaffolded — pnpm workspaces, TypeScript project references, esbuild. Node 24 and pnpm 10 are pinned (`.nvmrc`, `engines`, `packageManager`). Spikes 1–3 have run; conclusions live in each `spikes/*/README.md`. Spike 1 is now a permanent test at `packages/core/test/round-trip.test.ts`.

**Impl order steps 1–11 are done.** `apps/vscode/` registers a `CustomTextEditorProvider` (`facet.markdownEditor`) hosting a CodeMirror 6 webview. Edits flow webview → `postMessage` → `WorkspaceEdit` → `TextDocument`; saves write the buffer's bytes verbatim — the v1 structural mechanism for D5. esbuild builds two bundles (extension/Node CJS, webview/browser IIFE); tsconfigs are split so the webview source gets DOM lib without polluting the extension. `priority` is `"option"`, so VS Code's default markdown editor stays primary until impl order step 12 (the activation command). F5 dev loop is in `apps/vscode/.vscode/`; `apps/vscode/fixtures/sample.md` is the playground (and is `.prettierignore`'d so Prettier doesn't fight verbatim writes).

`@facet/core` exposes `parse(markdown): Root` (`packages/core/src/parse.ts`), `findInlineMarks(root): InlineMark[]` (`packages/core/src/find-inline-marks.ts`), `findBlocks(root): Block[]` (`packages/core/src/find-blocks.ts`), `findTables(root): Table[]` (`packages/core/src/find-tables.ts`), `findFrontmatter(root): Frontmatter | null` (`packages/core/src/find-frontmatter.ts`), `findImages(root): Image[]` (`packages/core/src/find-images.ts`), `slugifyHeading(text): string` and `slugifyHeadings(texts): string[]` (`packages/core/src/slugify-heading.ts` — GitHub-compatible: lowercase, trim, strip non-`\w\s-`, collapse whitespace to `-`, with `-1`/`-2` collision suffixes), plus `findHeadingLineForAnchor(markdown, anchor): number | null` (`packages/core/src/find-heading-line-for-anchor.ts` — parses, computes slugs, returns the 0-indexed line of the matching heading, or null on no match). Parse stack is `unified + remark-parse + remark-gfm + remark-frontmatter` — `remark-frontmatter` is configured for `["yaml"]` only (D6 lock — no TOML / `+++` fences) and is now a runtime `dependency`. `remark-stringify` remains a `devDependency`, used only by the round-trip test. `findInlineMarks` returns `strong`/`emphasis`/`link` nodes with outer offsets (`start`/`end`) and inner offsets (`innerStart`/`innerEnd`); links also carry `url`/`title`. `findBlocks` returns `heading` (with `depth` 1–6 and `markerStart`/`markerEnd` covering `# `…`###### `), `blockquote` (outer-only — nested blockquotes deduped to outermost to avoid duplicate per-line `>` decorations), `listItem` (with `ordered` flag, marker range covering `- ` or `1. ` — including any `[ ]` / `[x]` and its trailing space — plus `checked: boolean | null` and `checkboxStart`/`checkboxEnd: number | null` for GFM task lists; plain items get null defaults), and `code` (with `lang: string | null`). `findTables` returns each `table` with outer offsets, an `align` array (`"left"`/`"center"`/`"right"`/`null` per column), an `alignmentRow` range covering the `| --- |` separator (computed from the gap between mdast's header row end and first body row start, since mdast doesn't surface the separator as its own node), and `rows[]` carrying `isHeader` plus per-cell `start`/`end` offsets that share boundaries at the `|` separators. `findFrontmatter` returns `{ type: "yaml", start, end }` for the top-of-doc YAML node only, or `null` — mid-doc `---` fences parse as thematic breaks and are not surfaced. `findImages` returns each mdast `image` with outer `start`/`end` plus `url` / `alt` / `title` (alt defaults to `""`, title to `null`); reference-style `imageReference` nodes are excluded (D6 lock — only literal `![alt](url)` syntax). All helpers recurse through every block container, return doc order, and emit empty marker ranges for setext headings (caller must guard).

The webview consumes these via seven parallel StateFields. `apps/vscode/src/webview/inline-marks.ts` re-parses on each doc/selection change and uses `Decoration.replace` to hide `start..innerStart` and `innerEnd..end` when the cursor doesn't overlap a mark's outer range, plus `Decoration.mark` on the inner range with `.facet-strong` / `.facet-emphasis` / `.facet-link` classes. `apps/vscode/src/webview/block-marks.ts` does the same shape for headings, blockquotes, and list items: `Decoration.line` per block-line for `.facet-heading-line-{1..6}` / `.facet-blockquote-line` / `.facet-list-line` styling, plus `Decoration.replace` to hide `# `, `> `, and `- ` markers when the cursor's line is outside the block's line range (line-based check, not offset-based, so cursor at the end of a heading line still keeps the source visible). Plain unordered list items render their marker as a `• ` widget (`BulletWidget`) when not editing; ordered items keep `1. ` visible. GFM task list items render as a clickable checkbox: unordered tasks emit a single atomic `Decoration.replace` covering `markerStart..checkboxEnd` with a `CheckboxWidget` (combining the marker hide and the widget into one range so the cursor can't land in the hidden gap between them when arrowing up/down); ordered tasks keep `1. ` visible and replace just `[ ]` / `[x]`. Clicking the checkbox dispatches a CodeMirror change built by a pure-function `buildToggleEdit({checked, checkboxStart, checkboxEnd}) → {from, to, insert}` (`apps/vscode/src/webview/task-toggle.ts`) that produces an exact `[ ]` ↔ `[x]` swap; the existing webview → `postMessage` → `WorkspaceEdit` flow handles persistence so the save path stays untouched. `apps/vscode/src/webview/code-marks.ts` owns code blocks end-to-end: `.facet-code-line` / `.facet-code-fence-line` line decorations, per-token inline `Decoration.mark` highlighting via Shiki (`createHighlighterCore` from `shiki/core` with the `@shikijs/engine-javascript` JS regex engine — no WASM, CSP-clean — bundling 12 grammars statically: bash/css/go/html/javascript/json/markdown/python/rust/tsx/typescript/yaml, plus the `github-dark` and `github-light` themes), and `Decoration.replace({ block: true })` over both fences when the cursor is off the block — the opening fence's replace carries a `LangBadgeWidget` showing `block.lang`. View creation in `main.ts` awaits `highlighterReady` so the StateField sees a loaded highlighter on first render. Token marks use inline `style="color: ..."`. The Shiki theme name is no longer baked in: `code-marks.ts` exports `createCodeMarksField(theme: string)` (factory, not const) so the theme can be swapped at runtime via the `themeCompartment` in `main.ts`. `apps/vscode/src/webview/table-marks.ts` follows the same pattern for tables: `Decoration.line` for `.facet-table-line` / `.facet-table-header-line` / `.facet-table-alignment-line`, plus `Decoration.replace` to hide each `|` when the cursor is on a non-table line (cursor-in-table is line-based, mirroring blocks), plus `Decoration.mark` for `.facet-table-cell-{left,center,right}` on cells in non-default-aligned columns. `apps/vscode/src/webview/frontmatter-marks.ts` emits a single `Decoration.replace` covering `start..end` with a clickable `FrontmatterWidget` ("⌃ Frontmatter") when the cursor is off any frontmatter line; clicking dispatches a selection change to land the cursor inside the YAML body (mirroring the cursor-in-block reveal pattern). The field also exposes `EditorView.atomicRanges` so CM treats the collapsed range as a single jump unit — without that, arrow keys across the multi-line replace landed at unpredictable offsets due to CM's pixel-x goal-column logic. `apps/vscode/src/webview/image-marks.ts` mirrors the cursor-in-block reveal pattern for images: when the cursor is off the image's line, a `Decoration.replace` covers `start..end` with an `ImageWidget(url, alt, title)` that renders an `<img>`. URL resolution is injected: `buildImageDecorations(doc, selFrom, selTo, resolve)` takes a `resolve: (rawUrl) => string` so the StateField is unit-testable in isolation, and `createImageMarksField(resolve)` is a factory (rather than a const) because the resolver is established at view-creation time once the first `update` message arrives with `baseUri`. `apps/vscode/src/webview/resolve-image-url.ts` exposes the production resolver: absolute URLs (`https`, `http`, `data:`) pass through unchanged via the URL constructor; relative paths join against the base webview URI. Trailing-slash normalization included so `vscode-webview://.../dir` resolves the same as `.../dir/`. `provider.ts` computes the document directory's `webview.asWebviewUri()` string and ships it as `baseUri` on every `update` message, and widens `localResourceRoots` from just `dist/` to also include the workspace folder (or the document's directory if no workspace) so the webview can fetch local images. `main.ts` captures `baseUri` from the first message and constructs `(url) => resolveImageUrl(url, baseUri)`; falls back to identity if `baseUri` is missing (untitled docs render `https` images but not relative paths). `buildDecorations`, `buildCodeDecorations`, `buildTableDecorations`, `buildFrontmatterDecorations`, and `buildImageDecorations` are exported for cursor-boundary unit tests in `apps/vscode/test/`. The table alignment row is muted via opacity rather than collapsed, and the frontmatter widget's source line breaks still consume vertical space (parked: both want `Decoration.replace({ block: true })` later, the same mechanism step 7 used for code fences — see HANDOFF.md). All decorations are styled in `provider.ts`'s HTML using VS Code theme variables. `apps/vscode` hosts vitest alongside `@facet/core`'s test suite, so `pnpm -r test` runs both. **D8** (line-based reveal vs. overlay-layer rendering for complex blocks) is recorded in `docs/facet-decisions.md` with revisit triggers at steps 7 / 10 / 13 — neither step 7 nor step 8 fired the trigger (Shiki token marks, block-level fence collapse + lang-badge widget, and the inline image widget all fit the existing cursor-in-block reveal pattern), so the next D8 checkpoint is step 10 (Mermaid). **Step 9 (links)** added click-to-follow over the existing `.facet-link` decoration. Three small unit-tested webview helpers compose: `apps/vscode/src/webview/find-link-at.ts` exposes `findLinkAt(doc, offset): { url, start, end } | null`; `apps/vscode/src/webview/decide-link-click.ts` exposes `decideLinkClick(doc, clickOffset, selFrom, selTo): { follow: true, url } | { follow: false }` — declines to follow when the cursor was already inside the link's outer range, since that's source-mode positioning rather than navigation. `apps/vscode/src/webview/link-click.ts` is an `EditorView.domEventHandlers` extension hooked on **`mousedown`** (not `click`): CodeMirror moves the cursor on mousedown, so a `click` hook would always observe the post-move selection inside the link and decline — a bug that pure unit tests can't surface, the gap between "logic is correct" and "wired to the right event." Same-file anchors (`#section`) scroll within the existing webview via `EditorView.scrollIntoView` to avoid round-tripping through the extension and triggering a duplicate tab in VS Code's default text editor; cross-file links post `{ type: "openLink", url }` to the extension. `apps/vscode/src/classify-link-target.ts` exposes `classifyLinkTarget(rawUrl): { kind: "remote", url } | { kind: "internal", path, anchor }` (any URI scheme prefix → remote, fragment-only → internal with empty path, otherwise split on `#`). `provider.ts` routes remote URLs through `vscode.commands.executeCommand("vscode.open", uri)` and internal paths through `vscode.workspace.openTextDocument` + `vscode.window.showTextDocument`, with `revealRange` + `editor.selection` to the heading line for anchored cross-file links. CSS: `.facet-link` got `cursor: pointer`. Cross-file `.md` links currently land in VS Code's default markdown editor (Facet is `priority="option"`); step 12 resolves this. **Step 10 (Mermaid)** added a seventh StateField `apps/vscode/src/webview/mermaid-marks.ts` rendering fenced ` ```mermaid ` blocks as SVG diagrams via the cursor-in-block reveal pattern: cursor on the block shows raw source styled like a code block; cursor off collapses the entire fenced range with `Decoration.replace({ block: true })` carrying a `MermaidWidget`. The widget lazy-loads mermaid via dynamic `import("mermaid")` on first mount; `eq()` is keyed on `(code, theme)` so repeated mounts with unchanged source reuse the existing DOM (avoiding re-render flicker on selection-only changes) but theme changes bust the cache and force a re-render. The widget calls `mermaid.initialize({ theme: this.theme, securityLevel: "strict", startOnLoad: false })` before each `mermaid.render(...)` so theme switches take effect on the next paint. Click on a rendered diagram dispatches a CodeMirror selection change to the line after the opening fence; the click handler uses `view.posAtDOM(el)` to look up the live offset from the document rather than storing one in the constructor — that's what keeps `eq()` cheap and stable across doc edits. The StateField also provides `EditorView.atomicRanges` so arrow-up/down treats the multi-line block-replace as a single jump unit (mirroring the frontmatter pattern). `code-marks.ts` skips its fence-collapse and Shiki-tokens emission for `lang === "mermaid"` so the two StateFields don't fight; line classes still emit so the revealed source matches other code blocks visually. **D8 outcome at the step-10 checkpoint** (recorded in `docs/facet-decisions.md`): line-based reveal fits Mermaid cleanly — the overlay-layer pivot stays parked, next checkpoint moves to step 13 polish. **Bundle:** the webview is now **4.4 MB** because esbuild's IIFE format folds the dynamic `import("mermaid")` into the static bundle (lazy-load contract is broken architecturally, working in source); the proper fix is the ESM-webview refactor that closes both the step 7 (Shiki, 1.5 MB) and step 10 (Mermaid, 4.4 MB) parked items together, folded into step 13 polish. **Step 11 (theme integration)** wired VS Code's three theme kinds (light, dark, high-contrast — and high-contrast-light) through the webview. `apps/vscode/src/theme-kind.ts` (shared between extension and webview) exposes the `VsCodeThemeKind` string type plus three pure mappers: `pickShikiTheme(kind)` (`light`/`high-contrast-light` → `github-light`, otherwise `github-dark`), `pickMermaidTheme(kind)` (`dark` → `dark`, `light` → `default`, HC kinds → `neutral`), and `themeKindFromVsCodeEnum(1|2|3|4)` (extension-side, maps `vscode.ColorThemeKind`). `provider.ts` ships the kind on every `update` message and re-sends on `vscode.window.onDidChangeActiveColorTheme`; `main.ts` validates the incoming string, holds it in `activeThemeKind`, and uses a single `themeCompartment` (CM6 `Compartment`) wrapping `[createCodeMarksField(pickShikiTheme(kind)), createMermaidMarksField(pickMermaidTheme(kind))]` — `applyThemeKind(newKind)` dispatches `themeCompartment.reconfigure(...)` so existing views swap themes without rebuild. The `tsconfig.webview.json` `include` was widened to pick up `src/theme-kind.ts` from outside `src/webview/`. CodeMirror's gutter chrome (line numbers, active-line gutter) gets VS Code colors via a separate `vscodeTheme` `EditorView.theme()` extension — needs CM's theme system rather than top-level CSS because CM injects its default `.cm-gutters` styles at runtime and beats document-level `<style>` on specificity. The `.facet-mermaid-error` hardcoded `#f48771` fallback was dropped (now reads `var(--vscode-errorForeground)` only). Selection / cursor / active-line CM defaults are intentionally **not** themed — a first attempt flickered visibly during selection drag because reveal-on-cursor StateField rebuilds invalidate the selection layer mid-paint; parked for step 13 (see HANDOFF.md). **Step 12 (next)** is the activation command (`"Facet: Set as default markdown editor"`) plus a "View source" escape hatch — read HANDOFF.md "Immediate next action".

Common commands (run from repo root):

- `pnpm install` — install workspace deps.
- `pnpm -r build` — build all packages.
- `pnpm -r test` — run all tests.
- `pnpm -r typecheck` — typecheck all packages.
- `pnpm lint` — eslint the workspace.
- `pnpm format` / `pnpm -w format:check` — apply / verify Prettier.

CI runs format:check, lint, typecheck, build, and test on every push and PR (`.github/workflows/ci.yml`).

## Read these before doing substantive work

The design has converged in writing. Skim these in order — they encode constraints that aren't visible from code:

1. `HANDOFF.md` — current state and the immediate next action.
2. `CONTRIBUTING.md` — setup and command reference.
3. `docs/facet-vision.md` — what Facet is for. Check feature ideas against the "Convergence of understanding" articulation.
4. `docs/facet-spec-v1.md` — v1 scope for Facet for VS Code. Includes explicit non-goals; respect them.
5. `docs/facet-decisions.md` — decision log with revisit conditions. Don't relitigate without checking the "When to revisit" criteria.
6. `docs/UBIQUITOUS_LANGUAGE.md` — canonical glossary. Use these terms in code, comments, commits, and prose.

## Architecture in one paragraph

Facet is a family of **Surfaces** over the same Markdown file. V1 is **Facet for VS Code**, a `CustomTextEditorProvider` hosting a webview that runs **CodeMirror 6** in **Hybrid live-preview** mode. **Remark / unified** is the **parser** (not CodeMirror's built-in markdown), specifically so the AST stays consistent with the future web Surfaces (**Facet Review**, **Facet Studio**). In v1, `remark-stringify` is deliberately not used: saves write the CodeMirror buffer verbatim, which is what preserves round-trip fidelity (D5). The git repo is the **Content source of truth**; **Threads** and **Comments** (not in v1) will live in a separate **Collaboration source of truth**. Logic for parsing, frontmatter, and link resolution should live in `packages/core/` so it can be reused by future Surfaces — keep it out of CodeMirror plugins. `packages/core/` should expose parsing and AST queries; it deliberately does not expose markdown stringification in v1.

## Non-negotiable guardrails

These are easy to violate by accident and expensive to undo:

- **Round-trip fidelity (D5).** Saving must produce byte-identical markdown unless the Author actually changed the Document. The mechanism in v1 is structural: CodeMirror owns the text buffer and saves write its bytes verbatim — `remark-stringify` is not called on save. Don't add a save-path serializer. Don't let CodeMirror, Remark config, or any plugin silently normalize list markers, reflow paragraphs, or rewrite link forms. The behavior lock lives at `packages/core/test/round-trip.test.ts` — keep it green and don't disable it. `remark-stringify` is a `devDependency` of `@facet/core` for that test only; don't promote it to `dependencies` or import it from `packages/core/src/`.
- **No custom markdown syntax (D6).** CommonMark + GFM only. No wiki-style `[[links]]`, custom callouts, or embed syntax. Files must render correctly on GitHub, ADO, and any future Surface.
- **Use the canonical terms.** Especially: **Surface** (technical) vs **Facet** (brand), **Thread** vs **Comment**, **Author** vs "user", **Content source of truth** vs **Collaboration source of truth**. The bare word "editor" is overloaded — say **Surface**, **Author**, or name the library (CodeMirror 6, Milkdown) directly.
- **Plan B is real (D3).** If CodeMirror's Hybrid live-preview turns into disproportionate custom-plugin work, switching Facet for VS Code to **Milkdown with a source-mode toggle** is a legitimate move, not a failure. Keep the core (`packages/core/`) library-agnostic so the swap is feasible.
- **V1 is Facet for VS Code only.** Web Surfaces, Threads/Comments, real-time collab, properties panels, paste-image, and toggle-between-modes UX are all explicit non-goals. See the non-goals list in `docs/facet-spec-v1.md` before building anything that smells adjacent.
