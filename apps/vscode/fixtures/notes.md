# Notes — link target

This file exists so the sample can link to it.

## Setup

Pretend there are setup instructions here. Anchor link from `sample.md` should land at this heading.

## Setup

A duplicate heading to exercise the `-1` collision suffix. The link `notes.md#setup-1` should land here.

## Caveats

Another section. The link `notes.md#caveats` should land at this heading.
